copyright: halo
productName: linux-command
artifactName: ${productName}-${os}-${arch}-${version}.${ext}
files:
  - "main.js"
  - ".deploy/**"
asar: true
compression: maximum
nsis:
  perMachine: true
  oneClick: true
